# draughts-ai

Game of International Checkers with AI.

Tested on Linux and Windows 10 with Cygwin.

## Setup

- Make sure SDL, SDL_image, SDL_mixer, SDL_ttf are installed
- Edit the HPATH variable in the Makefile to include the path to your SDL packages
- Build with make all
- Execute PIR.exe